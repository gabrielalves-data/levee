# Dependencies
node_modules/
.pnp
.pnp.js

# Build outputs
dist/
dist-electron/
client/dist/
out/

# Electron packaged app
release/
*.dmg
*.exe
*.AppImage
*.deb
*.rpm
*.snap

# Environment & secrets
.env
.env.*
!.env.example

# Python virtualenv
.venv/
venv/
__pycache__/
*.py[cod]

# SQLite databases (user data lives at ~/.levee/, but guard local copies too)
*.db
*.sqlite
*.sqlite3
*.db-journal
*.db-wal
*.db-shm

# Levee runtime state — never belongs in the repo
.levee/
secrets.json
overlay-bounds.json

# Logs
logs/
*.log
npm-debug.log*
electron-debug.log

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor / tooling
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
.claude/
*.swp
*~

# Vite
.vite/
client/.vite/

# Tailwind / PostCSS cache
.postcss-cache/

# Testing
coverage/
.nyc_output/

# Misc
*.tsbuildinfo
.cache/

# Private working notes — docs/ is ignored wholesale so planning material
# (notes.txt, PROJECT-NOTES.md, levee-ssot.md) can never be published by
# accident. Public docs assets are opted back in explicitly, one by one.
notes.txt
docs/

# Local AI-assistant instructions — maintainer-only, not part of the project
CLAUDE.md